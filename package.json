{
  "name": "flags-sdk",
  "module": "index.ts",
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "lint": "oxlint packages --ignore-path .oxlintignore",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxlint": "^1.35.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
